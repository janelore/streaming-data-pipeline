val statesVisited = Map(
  "MO" -> "Arch",
  "IL" -> "Cahokia Mounds",
  "IN" -> "Racetrack"
)

val stateCodesOrdered = List("MO", "IL", "IN")
val placesVisited = stateCodesOrdered.map(statesVisited => )
}